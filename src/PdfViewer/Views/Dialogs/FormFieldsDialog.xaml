<Window x:Class="PdfViewer.Views.Dialogs.FormFieldsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Form Fields"
        Height="520" Width="720"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource SurfaceBrush}"
        ShowInTaskbar="False">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Form Fields"
                       FontSize="18" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextPrimaryBrush}" />
            <TextBlock x:Name="SubtitleText"
                       Margin="0,4,0,0"
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource TextSecondaryBrush}" />
        </StackPanel>

        <Border Grid.Row="1"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="4">
            <DataGrid x:Name="FieldsGrid"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      Background="Transparent"
                      RowBackground="Transparent"
                      BorderThickness="0"
                      Foreground="{DynamicResource TextPrimaryBrush}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Page" Binding="{Binding PageNumber}" IsReadOnly="True" Width="60" />
                    <DataGridTextColumn Header="Field" Binding="{Binding Name}" IsReadOnly="True" Width="2*" />
                    <DataGridTextColumn Header="Type" Binding="{Binding Type}" IsReadOnly="True" Width="110" />
                    <DataGridTextColumn Header="Value" Binding="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Width="3*" />
                    <DataGridCheckBoxColumn Header="Read-only" Binding="{Binding IsReadOnly}" IsReadOnly="True" Width="80" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <TextBlock x:Name="HintText"
                       VerticalAlignment="Center"
                       Margin="0,0,16,0"
                       Foreground="{DynamicResource TextSecondaryBrush}" />
            <Button x:Name="SaveButton"
                    Content="Save As..."
                    MinWidth="110" Padding="12,6"
                    Click="SaveButton_Click" />
            <Button Content="Close"
                    MinWidth="90" Padding="12,6" Margin="8,0,0,0"
                    IsCancel="True"
                    Click="CloseButton_Click" />
        </StackPanel>
    </Grid>
</Window>
